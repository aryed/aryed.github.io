# Google Antigravity Repository Instructions

Read and follow `AGENTS.md` before changing this repository. It is the canonical shared instruction file for both Google Antigravity and Codex.

Also read `PROJECT_STATUS.md` and any task-relevant document under `docs/`. In particular, read `docs/non-math-gallery-recovery.md` before modifying the Non math photo gallery.

Do not duplicate or independently redefine the shared rules here. Update `AGENTS.md` when a repository-wide working rule changes so both agents continue to use the same source of truth.

